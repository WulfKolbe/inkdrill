2026-08-21T11:54:10Z  /  2026-08-21 13:54:10 +02:00 (Europe/Berlin)  commit 2b2e816  (retrofitted: the commit that ADDED this report)
075 - deletion of the files 074 marked unread
==========================================================================

FREED: 642.7 MiB  (673,962,153 bytes)

     size  target
--------------------------------------------------------------------------
   410.5M  scratchpad/mpx          renders, superseded harness
   158.7M  scratchpad/bh2p54       bh2 renders, 3 regenerations stale
    59.8M  scratchpad/p13          abandoned locate-driver renders
    12.1M  scratchpad/ loose       110 crops, pgm/png, logs, drivers
   870.6K  scratchpad/bh2cmp       early compare output
   269.7K  scratchpad/cmp          early compare output
   283.0K  /tmp/*.bak              14 mutation-sweep restores
   126.6K  scratchpad/es3chk       ES3 regression crop
    23.0K  /tmp probe files        7 scripts and logs
     8.9K  scratchpad/ljcheck      driver, promoted to tools/

HELD BACK, and why -- three items, each named rather than
silently skipped:
--------------------------------------------------------------------------

  1.4G  scratchpad/p13cmp        MARKED READ IN 074
        tools/threeway.py hardcodes this path. Deleting it breaks a
        committed tool. The fix is a code change (give it
        reportcompare.py's $INKDRILL_WORK treatment); until then the
        directory is load-bearing. Deleting it would free more than
        everything above combined, which is exactly why 074 ran first.

  1.4G  /tmp/inkdrill-reportcompare   MARKED READ IN 074
        the compare harness's working directory. Every entry is stale
        (reports regenerated) and the harness clears stale files
        itself on the next run, so deleting it is SAFE but pointless:
        the next compare re-creates it. Left for the same reason a
        build cache is left -- it costs nothing to keep and 2 hours to
        rebuild by hand.

  2.1M  scratchpad/rescue        UNREAD, but flagged in 074 as the
        user's call and 075 was issued in the same message, so it
        cannot be a reply to that flag. It is the pyc snapshot taken
        when a git checkout destroyed another session's uncommitted
        work. Nothing reads it and the work was recovered, so it is
        deletable -- but it is evidence of an incident, 2.1M, and
        irreversible. Say the word and it goes.

  708K  <session>/tasks          background-command logs. Marked
        unread in 074 and it is, but the directory is harness-managed
        rather than mine, so I left it. Same one-word rule.

NOT TOUCHED, and not eligible: the session's deliverables in
~/pdfdrill-library -- 54 report.compare.tsv, 3,244 *.coverage.tsv,
4 *.profile.tsv. 074 named them so this step could not sweep them up.

VERIFIED AFTER DELETION:
  scratchpad now      1377M (p13cmp + rescue only)
  repo working tree   clean, nothing deleted inside it
2026-08-21T11:54:10Z  /  2026-08-21 13:54:10 +02:00 (Europe/Berlin)  end (retrofitted: finish time not recorded)
